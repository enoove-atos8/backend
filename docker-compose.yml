version: '3'
services:
    backend:
        image: enoove/atos242-backend:'${DEV_API_VERSION}'
        ports:
            - "8000:8000"
        container_name: backend
        networks:
            - atos242-backend-network

networks:
    atos242-backend-network:
        driver: bridge
